component:
  name: read_production_schedule
  description: Read production schedule data from the local data directory.
  read:
    connection: read_local_files
    local_file:
      path: read_production_schedule.csv
      parser:
        csv:
          header: true